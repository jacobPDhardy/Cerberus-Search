Make algorithm support multi level operations - DONE
Create top level validation checks
Add my escape algorithm to top level bracket parser. - DONE
Remove redundant sorts and dupe removes to speed it up.
Add global not to operations.
Create GUI + tabs + history + caching results for quicker searches
Create gui notifcations for invalid input and remove error throws

Create algorithm to simplify operations to make as effiecent as possible - maybe? will be very difficult.
Convert methods to support T 
Convert uses of List to IEnumerable and IQueryable where possible
